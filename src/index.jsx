import React from 'react';
import ReactDOM from 'react-dom';
import { Application } from './Application';

const domElement = document.getElementById('app-root');
ReactDOM.render(<Application />, domElement);
